services:
  - type: web
    name: michael-jackson-top-15
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3000
      - key: DB_PATH
        value: /data/app.db
      - key: SPOTIFY_CLIENT_ID
        sync: false
      - key: SPOTIFY_CLIENT_SECRET
        sync: false
    disk:
      name: mj-top15-data
      mountPath: /data
      sizeGB: 1
